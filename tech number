num=int(input("enter a number:")
str=str(num)
length=len(str)
if length%2!=0:
  print(f"{num} is not a tech number")
else:
  first=int(str[:length//2])
  second=int(str[length//2:])
  total=first=second
  if total*total==num:
     print(f"{num} is tech number")
  else:
     print(f"{num} is not tech number") 
