valid=lambda p:(
len(p)>8 and
any(c.isalpha() for c in p) and
any(c.isdigit() for c in p)
)

password=input("enter the password:")
print(password)
if valid(password):
  print("entered password is valid")
else:
  print("enterd password is invalid")
